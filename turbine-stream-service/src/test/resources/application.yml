server:
  port: 8989

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics

spring:
  cloud:
    stream:
      binders:
        rabbit:
          type: rabbit

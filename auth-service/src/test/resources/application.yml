spring:
  data:
    mongodb:
      database: piggymetrics
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.9

eureka:
  client:
    enabled: false

server:
  port: 0

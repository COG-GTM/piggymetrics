spring:
  data:
    mongodb:
      database: piggymetrics
  cloud:
    config:
      enabled: false
  main:
    allow-bean-definition-overriding: true
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.0
